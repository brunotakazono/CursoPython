''' Modulos adicionar funcionalidades por meio do import e nome da biblioteca.
import + biblioteca  importa todos as funcionalidades do modulo.
from biblioteca import modulo importa apenas o modulo especificado da biblioteca.'''
#from math import sqrt,floor
#num = int(input('Digite um número: '))
#raiz = sqrt(num)
#print(f' A raiz quadrada de {num} é {raiz:.2f}'.format(floor(raiz)))

import random
num = random.randint(1, 100)
print(num)
